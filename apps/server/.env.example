# Local development uses the SQLite schema and this local file.
DATABASE_URL="file:./dev.db"

# Optional. Leave unset to use only the deterministic parser.
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-flash-latest"

PORT=4000
